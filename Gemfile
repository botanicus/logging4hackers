# encoding: utf-8

source 'https://rubygems.org'

gem 'coderay'

group(:test) do
  gem 'rspec'
end

group(:documentation) do
  gem 'yard'
  gem 'redcarpet'
end

group(:release) do
  gem 'changelog'
end
